# Galaxea R1 Pro软件版本 - V2.0.4 - 更新日志

## 更新说明

**产品名称：**Galaxea R1 Pro

**软件版本号：**V2.0.4

**Ubuntu 系统：** 22.04 LTS

**ROS版本：** ROS 2 Humble

**发布日期：**2025.5.15

**更新项：**

1. **驱动内容更新项：**

    a）移植ROS2 `check_node`功能；

    b）修复底盘相机启动问题；

    c）更新底盘VCU固件，修复其在更改模式时发生急停。

2. **遥操作功能更新项：**

    a）上线ROS2 R1 Pro-T同构遥操[SDK及文档](../../R1Pro_Teleop_Usage_Tutorial.md)
    
    b）修复左右腕部相机Topic名称为`realsense_camera`，对齐ROS2软件文档中的接口定义。

3. **工具模块更新项：**

    a）新增文件`startup_config`中R1 Pro-T同构遥操作的启动脚本；

    b）优化文件`data_collection`中bag存储格式为`MCAP`，需要依赖`ros-humble-rosbag2-storage-mcap`包。

5. **其他内容更新项：**

    a）更新包含SDK基础依赖的ROS2系统镜像的烧录方式。


## 更新说明

1. 在升级过程中，请不要中止系统运行，也不要进行任何控制操作，否则可能导致升级失败，影响系统正常使用。
2. 升级完成后，请关闭机器人电源，并重新进行上电操作，以确保系统更新完全生效。

**请严格按照以下教程完成本次系统更新，如遇问题，请及时联系我们至 [support@galaxea.ai](mailto:support@galaxea.ai)获得技术支持!**


## 更新教程
### 下载更新包

`atc_standard-V2.0.4.tar.gz`

`atc_navigation_V2.0.4.tar.gz`

`atc_host_standard_V2.0.4.tar.gz`

- 百度云：[ATC_SDK_V2.0.4](https://pan.baidu.com/s/1Qewogh-_Xa_wJ1zgcBWRSQ?pwd=v204)
- Google Drive：[ATC_SDK_V2.0.4](https://drive.google.com/drive/folders/1ZQX9nKik3OO9LzeD6MqQF0x8Mf7VN5YD?usp=sharing)

### 解压更新包

```Bash
tar -xf ${your_download_path}/atc_standard-V2.0.4.tar.gz -C ~/
```

### 启动CAN通信

```Bash
# 终端1
sudo ip link set can0 down
sudo ip link set can0 type can bitrate 1000000 dbitrate 5000000 fd on       
sudo ip link set can0 up
```

### 更新嵌入式固件

```Bash
# 终端2
cd ~/ && source install/setup.bash
cd ~/install/Embedded_Software_Firmware/share/tools
cd R1PRO
./r1pro_embedded_firmware_upgrade.sh ls ../../firmware/R1PRO
```

等待几秒后，出现“GET OTA finished!！”即表示更新成功。
![sdk_ros2_xcu_cn](../assets/sdk_ros2_xcu_cn.png)

<span style="color:red;">**注意：更新完毕后，请关闭机器人电源并重新上电。**</span>

### 运行整机功能

1. **在`atc_standard-V2.0.4.tar.gz`中，启动本体`R1PRO BODY`：**
    ```Bash
    cd install/startup_config/share/startup_config/script/
    ./robot_startup.sh boot ../sessions.d/ATCStandard/R1PROBody.d/
    ```

2. **在`atc_standard-V2.0.4.tar.gz`中，启动VR遥操作功能`R1PRO VRTELEOP`：**

    首先，在`~/.bashrc`文件末尾添加以下环境变量，即可连接机器和VR眼镜： 
    ```Bash
    ROS_IP=机器人的无线网卡IP
    VR_IP=连接同一wifi的VR眼镜的IP
    ```

    然后，启动VR遥操作：
    ```Bash
    cd install/startup_config/share/startup_config/script/
    ./robot_startup.sh boot ../sessions.d/ATCStandard/R1PROVRTeleop.d/
    ```

3. **在`atc_navigation-V2.0.4.tar.gz`中，启动定位导航系统功能`R1PRO NAVIGATION`:**
    ```Bash
    cd install/startup_config/share/startup_config/script/
    ./robot_startup.sh boot ../sessions.d/ATCNavigation/R1PROVRTeleopNAV.d/
    ```

4. **在`atc_host_standard-V2.0.4_x86.tar.gz`中，启动R1 Pro-T遥操作：**

    首先，在R1 Pro内启动`atc_standard-V2.0.4.tar.gz`文件内的所有R1 Pro-T节点：
    ```Bash
    cd install/startup_config/share/startup_config/script/
    ./robot_startup.sh boot ../sessions.d/ATCStandard/R1PROBody.d/
    ```

    启动FDCAN通信：
    ```Bash
    sudo ip link set dev can0 type can bitrate 1000000 dbitrate 5000000 fd on
    sudo ip link set up can0
    ```

    然后，在X86主机上启动`atc_host_standard-V2.0.4_x86.tar.gz`文件内的同构遥操作：
    ```Bash
    cd install/startup_config/share/startup_config/script/
    ./robot_startup.sh boot ../sessions.d/ATCHostStandard/R1PROT.d/
    ```

### 关闭整机功能
执行以下命令，关闭上述操作开启的全部进程。

```Bash
tmux kill-server
```